# 全面修复计划

## 问题分析
根据您的反馈，需要修复以下 6 个问题：

---

## 步骤 1：修复导出功能
**文件**: `backend/main.py`

- **问题**: 导出格式与导入格式不匹配
- **修复**:
  - 导出字段改为：`name`, `nickname`, `remark`, `greeting`
  - 如果备注为空，导出空字符串（保持列一致性）
  - 确保导出的 CSV 可以直接导入回来

---

## 步骤 2：修复导入功能
**文件**: `backend/main.py`

- **问题**: 导入时第一行标题被当作数据导入，且祝福语未被保留
- **修复**:
  - 自动检测并跳过标题行（包含 name/nickname 等关键字）
  - 读取 `greeting` 列并保留
  - 支持 `remark` 列

---

## 步骤 3：添加联系人操作回调
**文件**: `frontend/components/ContactList.tsx`

- **新增**: 每个联系人卡片添加操作按钮
  - 编辑按钮（修改名称、备注）
  - 删除按钮
- **回调**: 添加 `onEdit` 和 `onDelete` 回调

---

## 步骤 4：创建编辑联系人模态框
**文件**: `frontend/components/EditContactModal.tsx` (新建)

- 功能：
  - 编辑好友名称
  - 编辑备注
  - 保存/取消按钮

---

## 步骤 5：添加删除确认和撤销功能
**文件**: `frontend/app/page.tsx`

- **删除功能**:
  - 删除前确认提示
  - 删除后显示 toast 提示
  
- **撤销功能**:
  - 添加 `deletedContacts` 状态存储最近删除的联系人
  - 删除后 5 秒内显示"撤销"按钮
  - 点击撤销恢复删除的联系人

---

## 步骤 6：集成所有功能到主页面
**文件**: `frontend/app/page.tsx`

- 添加 `handleEditContact` 函数
- 添加 `handleDeleteContact` 函数
- 添加 `handleUndoDelete` 函数
- 集成 EditContactModal
- 集成撤销 toast

---

## 修改文件清单

| 文件 | 操作 |
|------|------|
| `backend/main.py` | 修改导出和导入逻辑 |
| `frontend/components/ContactList.tsx` | 添加编辑/删除按钮 |
| `frontend/components/EditContactModal.tsx` | 新建 |
| `frontend/app/page.tsx` | 集成所有功能 |