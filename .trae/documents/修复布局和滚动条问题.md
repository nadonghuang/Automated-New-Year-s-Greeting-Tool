### 修改 1: `globals.css` - 添加滚动条隐藏工具类

在文件末尾添加 `scrollbar-hide` 工具类：
```css
@utility scrollbar-hide {
  -ms-overflow-style: none;
  scrollbar-width: none;
  &::-webkit-scrollbar {
    display: none;
  }
}
```

### 修改 2: `page.tsx` - 右侧容器移除滚动限制

将第 480 行：
```tsx
<div className="flex-1 overflow-y-auto relative z-10 scrollbar-hide">
```
改为：
```tsx
<div className="flex-1 relative z-10 overflow-hidden">
```

### 修改 3: `ContactList.tsx` - 确保左侧有滚动

当前第 99 行已有 `overflow-y-auto scrollbar-hide`，添加 `scrollbar-hide` 后即可隐藏滚动条

### 修改 4: `GreetingReview.tsx` - 移除内部滚动

将第 61 行：
```tsx
<div className="flex-1 overflow-y-auto scrollbar-hide">
```
改为：
```tsx
<div className="flex-1 overflow-hidden">
```

这样：
- ✅ 右侧贺词正常显示，无高度限制
- ✅ 左侧好友列表可滚动
- ✅ 白色滚动条完全隐藏
- ✅ 内容不会被截断