## 后端问题 (20个)

### main.py
1. **临时文件未清理** - `export_contacts` 创建的临时文件永不删除
2. **文件上传缺少类型验证** - 只检查大小，不检查扩展名
3. **pandas 解析错误未处理** - 可能导致 500 错误
4. **parse_manual 缺少速率限制**
5. **用户输入未消毒** - 联系人名称可能包含危险字符

### generator.py
6. **基础问题数量不一致** - 代码写 `count - 11` 但前端只有 9 道基础题
7. **API 调用无超时设置**
8. **无重试逻辑**
9. **模型 ID 未验证**

### wechat_service.py
10. **全局变量无线程安全**
11. **空 except 块** (第114行)
12. **登录失败未清理状态**
13. **itchat.pkl 路径硬编码**

### config.py
14. **API Key 明文存储内存**

## 前端问题 (30个)

### page.tsx
15. **`lastAnswers` 状态定义但从未使用**
16. **`deletedContacts` 无限增长**
17. **localStorage 保存无防抖**
18. **导出操作无 loading 状态**
19. **配置弹窗不验证 API Key 格式**
20. **`handleSaveGreeting` 未验证 selectedContact 存在**
21. **`handleSelectTask` 未验证任务存在**

### Questionnaire.tsx
22. **网络请求无 AbortController**
23. **`customAnswer` 问题切换时未清空**
24. **AI 深度阶段进度百分比可能错误**
25. **无最大重试次数限制**

### GreetingReview.tsx
26. **useEffect 依赖数组不完整**
27. **剪贴板操作在非 HTTPS 下可能失败**
28. **initialGreeting 未验证**

### ContactList.tsx
29. **搜索过滤每次渲染创建新数组**
30. **长列表无虚拟化**

### FileUpload.tsx
31. **上传前不验证文件类型**
32. **无文件大小提示**
33. **拖拽无视觉反馈**

### ManualInput.tsx
34. **文本输入无字符限制**
35. **解析时 loading 状态不明显**

### LoginQRCode.tsx
36. **Interval 类型应使用 `ReturnType<typeof setInterval>`**
37. **组件卸载时可能未清理**

### layout.tsx
38. **`lang="en"` 应改为 `lang="zh-CN"`**

### globals.css
39. **缺少 focus-visible 无障碍样式**

### 通用问题
40-50. **其他优化**:
   - 缺少 Error Boundary 组件
   - 缺少 TypeScript 严格模式检查
   - 缺少单元测试配置
   - 缺少 SEO 优化
   - 缺少 PWA 支持
   - 缺少 ESLint 配置
   - 缺少 .env.example 文档
   - 缺少 API 响应缓存
   - 缺少请求取消机制
   - 缺少性能监控

## 修复优先级
1. **高**: #6 (基础问题数量不一致), #1 (临时文件), #10 (线程安全)
2. **中**: 所有前端 bug 和 UX 问题
3. **低**: 配置和架构优化